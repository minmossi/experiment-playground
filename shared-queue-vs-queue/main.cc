#include <cstdio>

#include <pthread.h>

#include "buffer.hh"
#include "region.hh"

int main(int argc, char *argv[]){
    // Command
    if(argc != 3){
        printf("usage: %s [QUEUE SIZE] [REGION SIZE]\n", argv[0]);
        exit(0);
    }

    // Create Buffers and Regions


    // Create Threads


    // Do Each Random Communication Simultaneously


    return 0;
}